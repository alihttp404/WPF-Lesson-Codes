<Window x:Class="Progressbar__Checklist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Progressbar__Checklist"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Slider
            x:Name="Sl"
            Width="300"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            ValueChanged="Sl_ValueChanged"
            Margin="0 20 0 0"
            Maximum="100">
        </Slider>

        <ProgressBar
            x:Name="Pb"
            Width="300"
            Height="20">
        </ProgressBar>

        <StackPanel x:Name="TrueFalseStackPanel">
            <RadioButton 
                x:Name="TrueRadioBtn" 
                Content="T" 
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10 5 0 0" 
                IsChecked="True"
                 />
            <RadioButton 
                x:Name="FalseRadioBtn" 
                Content="F"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10 0 0 0"
                />
        </StackPanel>
    </Grid>
</Window>
